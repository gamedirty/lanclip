{
  "$schema": "https://schema.tauri.app/capability/default",
  "identifier": "default",
  "description": "主窗口与接收弹窗的默认权限（自定义命令不在此列，天然可用）",
  "windows": ["main", "popup"],
  "permissions": ["core:default"]
}
